import { DisableAfterClick } from './disable-after-click';

describe('DisableAfterClick', () => {
  it('should create an instance', () => {
    const directive = new DisableAfterClick();
    expect(directive).toBeTruthy();
  });
});
